<%= t 'globals.app_name_plain' %>

------------------------------------

<%= t '.greetings', name: @resource.short_name %>

<%= t '.instruction', invited_by: @resource.invited_by.to_s %>

<%= t '.instruction_2', due_date: I18n.l(@resource.invitation_due_at, format: :long) %>

<%= t '.instruction_3' %>

<%= accept_invitation_url(@resource, invitation_token: @token) %>

------------------------------------

<%= t '.regards_plain' %>
